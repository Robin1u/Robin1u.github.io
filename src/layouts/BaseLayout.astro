---
export interface Props {
  title: string;
  description?: string;
  activePage?: string;
}
const { title, description = '', activePage = 'home' } = Astro.props;
---
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <title>{title} · Robin Lu</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300..800&family=JetBrains+Mono:wght@400;600&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="scroll-progress" id="scrollProgress"></div>

  <header class="mobile-header">
    <span class="mobile-header-name">Robin Lu</span>
    <button class="mobile-menu-btn" id="menuBtn" aria-label="打开菜单">
      <span></span><span></span><span></span>
    </button>
  </header>

  <div class="overlay" id="overlay"></div>

  <div class="layout">
    <aside class="sidebar" id="sidebar" role="navigation">
      <div class="sidebar-logo">
        <div class="name">Robin Lu</div>
        <div class="title"><span class="status-dot"></span>学生 / 设计师 / 开发者</div>
      </div>
      <nav class="sidebar-nav">
        <div class="nav-section">
          <div class="nav-section-label">导航</div>
          <a href="/" class:list={["nav-link", { active: activePage === 'home' }]}>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
            首页
          </a>
          <a href="/portfolio" class:list={["nav-link", { active: activePage === 'portfolio' }]}>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="3" width="20" height="14" rx="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
            作品集
          </a>
          <a href="/thoughts" class:list={["nav-link", { active: activePage === 'thoughts' }]}>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
            想法
          </a>
          <a href="/channel" class:list={["nav-link", { active: activePage === 'channel' }]}>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="23 7 16 12 23 17 23 7"/><rect x="1" y="5" width="15" height="14" rx="2"/></svg>
            频道
          </a>
        </div>
      </nav>
    </aside>

    <main class="main-content" role="main">
      <slot />
    </main>
  </div>

  <script src="/js/main.js" is:inline></script>
</body>
</html>